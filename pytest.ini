[pytest]
testpaths = scripts
python_files = test_*.py
markers =
    unit: Pure logic tests; no database or running API required.
    integration: Pipeline, database, or API tests (Postgres required for DB cases).
    qrt: Quality requirement verification tests (Postgres required).

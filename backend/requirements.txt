# =====================================================================
# Core Backend Dependencies
# =====================================================================
fastapi[all]
uvicorn
pydantic
python-multipart          # обработка multipart/form-data (загрузка аудио)
sqlalchemy
psycopg[binary]           # драйвер PostgreSQL (postgresql+psycopg)

# =====================================================================
# Authentication & Security
# =====================================================================
pwdlib[bcrypt]
PyJWT>=2.10.0
setuptools>=83.0.0          # PYSEC-2026-3447 (pip-audit)
openpyxl                  # экспорт статистики в Excel

# =====================================================================
# Audio, ML & ASR Toolchain
# =====================================================================
faster-whisper            # русская ветка (Whisper large-v3) + VAD
numpy
torch>=2.13.0              # >=2.13: совместим с setuptools>=83 (PYSEC-2026-3447)
transformers              # mms-lid-256 (LID) + yasalma/whisper-finetuned-tt-asr (tt)
accelerate                # потоковая загрузка весов (меньше пик памяти при load)
speechbrain               # диаризация: ECAPA speaker-эмбеддинги
scikit-learn              # диаризация: агломеративная кластеризация сегментов
pymorphy3                 # опционально: точнее определять русские слова (lang_tag)

# =====================================================================
# CI/CD Quality and Testing Automation (Required for Assignment 4)
# =====================================================================
pytest                    # Фреймворк тестирования
pytest-cov                # Инструмент замера покрытия строк (Line Coverage)
httpx                     # HTTP-клиент для интеграционных тестов FastAPI
pip-audit                 # Проверка уязвимостей (Additional QA Check)
ruff                      # Высокопроизводительный линтер (Source-code linting)
black                     # Автоматическое форматирование кода (Formatting check)
